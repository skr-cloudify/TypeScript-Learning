// Duck typing" or "Structural subtyping
export interface ICircleTypes {
    radius: number;
}

// Duck typing" or "Structural subtyping
export interface IRectangleTypes {
    length: number;
    width: number;
}
